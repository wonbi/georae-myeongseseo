# 거래명세서 웹앱 — 작업 맥락

이 파일은 Claude 가 자동으로 읽습니다. 집·회사·웹 어디서 시작하든 이 내용부터 파악하세요.

## 최근 작업 (2026-07-28)

- 상단 배너를 "마스터 거래명세서" 로 변경, v1.9 로 빌드·배포 완료
- 계정에 Claude GitHub 앱을 설치해서 웹 세션에서도 푸시가 됩니다.
  웹 세션은 main 에 직접 못 올리므로 작업 브랜치 → PR → 병합 순서로 반영하면 됩니다

## 무엇을 만드는가

수산물 유통사에서 쓰는 **거래명세서 작성 웹앱**입니다.
사용자는 거래처 발주를 받아 출고하는 업무를 하고, 일부 거래처가 거래명세서를 요구합니다.

- 배포 주소 : https://wonbi.github.io/georae-myeongseseo/
- 저장소 : 이 저장소 (public)

## 정산 규칙 — 가장 중요, 절대 바꾸지 말 것

**공급가가 곧 최종가입니다. 세액을 계산하지 않습니다.**

- 공급가 = 단가 × 수량. 그게 전부입니다
- 과세/면세는 **금액에 영향이 없는 분류 표시**일 뿐입니다.
  명세서 `세` 칸 표기와, 하단의 과세 품목 / 면세 품목 합계 집계에만 씁니다
- 합계금액 = (단가 × 수량)의 합 + 택배비 — 이 등식은 언제나 성립해야 합니다
- 택배비는 품목별 택배비 × 택배 건수를 합산합니다 (창고가 다르면 건별로 나가기 때문)

부가세 별도로 만들었다가 한 번, 부가세 포함가로 역산했다가 또 한 번 정정받은 내용입니다.
**세액 계산을 되살리지 마세요.**

정산 규칙을 바꾸게 되면 `index.html` 의 `DRAFT_V` 를 올려야 합니다.
안 그러면 브라우저에 남은 예전 임시저장본이 옛 설정을 되살려 금액이 틀리게 나옵니다.

## 개발·배포 절차

```bash
git pull                 # 반드시 먼저. 다른 장소에서 한 작업이 있을 수 있음
                         # index.html 수정
python build.py          # docs/ 갱신 + 버전 자동 증가
git add -A && git commit -m "..." && git push
```

- 소스는 **저장소 루트의 `index.html`**. `docs/` 안은 빌드 결과이니 직접 고치지 마세요
- `build.py` 가 products.js 인라인, 버전 증가, docs/ 생성을 모두 처리합니다
- GitHub Pages 는 `main` 브랜치의 `/docs` 를 서빙합니다. 푸시 후 30초쯤 걸립니다
- Pages **설정을 바꿨을 때는** 새 빌드를 직접 요청해야 반영됩니다
  `gh api --method POST repos/wonbi/georae-myeongseseo/pages/builds`

## 상품 데이터

공급처가 관리하는 구글시트에서 가져옵니다. 시트는 창고별 시트 여러 개 + '변동공지(상품 변동사항)' 시트로
되어 있습니다. 현재 원본은 드라이브의 **`마스터 외부유통 관리시트`** 이고, 변동분은 `[상품] 2. 변동공지`
시트에 쌓입니다.

받아오는 방법은 두 가지입니다.

```bash
curl -sL "<웹 앱 URL>" -o fetched.json   # 권장. 시트에 붙여넣어둔 apps-script.gs 가 응답
python fetch_products.py fetched.json
```

시트가 700KB 가 넘어서 **드라이브 API 로 통째로 읽으면 대화 용량을 넘깁니다.** 반드시 위처럼
파일로 내려받아 처리하세요. 연동 주소가 없으면 `parse_sheet.py <시트덤프.txt>` 를 씁니다.

단가 규칙은 `apps-script.gs` 와 `parse_sheet.py` 양쪽에 같은 내용으로 들어가 있습니다.
**한쪽만 고치면 두 경로의 단가가 갈립니다.** `node test_apps_script.js` 로 규칙을 검증할 수 있습니다.

**단가 결정 규칙** (틀리면 정산이 틀어집니다):

1. '상품 변동사항' 시트가 최우선. 같은 상품이 여러 번 나오면 **적용날짜 > 기재날짜** 순으로 최신 행
2. 없으면 창고별 마스터 시트 값
3. 상품은 **(창고명, 상품명)** 으로 구분. 창고가 다르면 이름이 같아도 다른 상품입니다
4. 문서에 나중에 나온 값을 쓰면 안 됩니다 (돌문어가 14,000 대신 22,000 으로 잡혔던 원인)

## 남은 작업

구글시트에 **같은 창고 안에서 단가가 두 개로 갈리는 상품이 9건** 있습니다.
시트 자체의 모호함이라 자동으로 못 정하고, 사용자가 시트를 직접 수정하기로 했습니다.

토막 은갈치 대짜 350g급(제수) 14,000/16,600 · 토막 은갈치 특대 450g급(제수) 24,500/27,000 ·
반건조 참가자미 4미(제수) 9,000/9,700 · 통통 쫄쫄이 오징어 400g(쫄깃) 26,500/29,500 ·
임금님 5-6미 전복 선물세트(완전) 36,000/39,000 · 한우 소머리스지 전골 1.7kg(우리) 20,000/21,000 ·
왕 바지락 1kg(이성) 4,000/5,000 · 원뿔 한우 꽃등심 200g(창원) 18,000/18,500 ·
맛상 미니족발 300g(하남) 3,000/3,500

시트가 정리되면 다시 파싱 → 빌드 → 푸시하면 됩니다.

## 알아둘 것

- **저장소가 public 인 이유** : 무료 플랜은 비공개 저장소에 Pages 를 못 붙입니다.
  공급가가 노출된다는 점을 알린 뒤 사용자가 공개를 선택했습니다. 다시 문제 제기하지 마세요
- 사업자등록증 인식(OCR)은 `docs/vendor/` 의 라이브러리를 씁니다. `file://` 로 열면 동작하지 않고
  웹 주소나 로컬 서버로 열어야 합니다
- 앱에 저장되는 거래처·공급자 정보는 **브라우저별 저장(localStorage)** 이라 PC 간에 공유되지 않습니다
- 사용자는 개발자가 아닙니다. 명령어를 알려줄 때는 복사해서 바로 붙여넣을 수 있게 주세요
